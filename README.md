Check if something has been required or not
===========================================

```ruby
require 'is_required'
require 'thread'

required? 'thread' # => true
```

The gem name is `is_required`
